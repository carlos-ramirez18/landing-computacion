# Copiar este archivo a ".env" y completar cuando corresponda.
# El ".env" real NUNCA se commitea (ver .gitignore).

# URL base de la futura API REST (Spring Boot). No se usa en la Etapa 1.
VITE_API_URL=http://localhost:8080/api

# Número de WhatsApp del negocio, formato internacional sin "+" ni espacios.
VITE_WHATSAPP_NUMBER=549XXXXXXXXXX
